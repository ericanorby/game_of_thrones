<div class="flex-space">
  <p><span>&#8606; </span><%= link_to "Back", houses_path %></p>
  <div>
    <p><%= link_to "Add Character", new_house_character_path(@house) %><span> + &nbsp;&nbsp;&nbsp;</span><%= link_to "Edit", edit_house_path %><span> | </span><%= link_to "Delete", house_character_path(@house), method: :destroy %></p>
  </div>
</div>

<h1><%= @house.name %></h1>
<h3>"<%= @house.words %>"</h3>

<div class="flex">
  <% @characters.each do |character| %>
    <div class="container">
      <p><%= link_to character.name, house_character_path(@house, character) %></p>
      <img class="small-photo" src="<%= character.photo_url %>">
    </div>
  <% end %>
</div>
